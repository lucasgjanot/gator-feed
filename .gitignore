# Node modules e lockfiles
node_modules/
npm-debug.log*
yarn-error.log*

# Arquivos de ambiente (senhas, chaves, variáveis sensíveis)
.env
.env.local
.env.*.local

# Build outputs e caches (se usar)
dist/
build/
.cache/

# Logs
logs/
*.log
npm-debug.log*

# Ignorar arquivos temporários do sistema
.DS_Store
Thumbs.db

# Ignorar arquivos que não devem ir pro repositório no Drizzle

# Mantenha as migrações SQL e meta do Drizzle, pois são essenciais para o histórico
# Então NÃO ignore nada dentro de:
# src/lib/db/migrations/
# src/lib/db/migrations/meta/

# Se tiver lockfiles ou arquivos temporários dentro de migrations/meta, ignore-os:
src/lib/db/migrations/meta/*.lock
src/lib/db/migrations/meta/*.tmp

# Se você gerar dumps locais do banco, ignore (não presente na sua estrutura atual)
# dumps/

# Evite versionar dados do banco em si (mas pelo seu tree, você não tem)

# Arquivos de editor (opcional)
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln

# Typescript
*.tsbuildinfo
